{
  "name": "homehuddle-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor iOS shell that packages the EXISTING HomeHuddle web app for TestFlight / App Store. Assemble, don't rebuild — the web UI in /homehuddle is reused verbatim.",
  "scripts": {
    "build:web": "bash ./build-www.sh",
    "sync": "npm run build:web && npx cap sync ios",
    "add:ios": "npx cap add ios",
    "open:ios": "npx cap open ios",
    "copy": "npx cap copy ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/browser": "^6.0.0",
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2",
    "@capacitor/push-notifications": "^6.0.2",
    "@revenuecat/purchases-capacitor": "^9.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
