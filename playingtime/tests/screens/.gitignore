# Screenshots are regenerated by every acceptance run. They are evidence for the
# run that produced them, not source, so they are not committed — a 1.4 MB binary
# churn on each test run would be noise in every diff.
*.png
